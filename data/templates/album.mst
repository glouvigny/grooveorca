<div>
    <ul>
        {% for track in data.tracks %}
            <li class="{{ track.id|eq(current_song.track_id)|_if("strong", "") }}">
                {{ track.track_position }} - {{ track.name }}
            </li>
        {% endfor %}
    </ul>
</div>