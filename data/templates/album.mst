<div>
    <ul>
        {% for track in album.tracks %}
            <li class="{{ track.id|eq(trackId)|_if("strong", "") }}">
                {{ track.track_position }} - {{ track.name }}
            </li>
        {% endfor %}
    </ul>
</div>