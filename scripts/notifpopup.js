var getArg = function(p){    return (match = window.location.search.match(new RegExp("[?|&]?" + p + "=([^&]*)"))) ? match[1] : false;}notifDuration = parseInt(localStorage.getItem('notificationsDuration'));notifDuration = 10;if(notifDuration < 3)	notifDuration = 3;var timeoutID;	var closeNotif = function(){	if (timeoutID) {		clearTimeout(timeoutID);		timeoutID = null;	}	window.close();};var handleMouseOver = function () {	clearTimeout(timeoutID);	timeoutID = null;};var handleMouseOut = function () {	if (timeoutID) {		clearTimeout(timeoutID);		timeoutID = null;	};	timeoutID = setTimeout(closeNotif, notifDuration * 1000);};var deezerControl = function(action) {	alert(action);	/*chrome.windows.getAll({populate : true},function(windows) {		for(var i = 0; i < windows.length; i++) {			var j = 0;			for(var dumb in windows[i].tabs) {				chrome.tabs.sendRequest(windows[i].tabs[j].id, {name: "controlDeezer", action: action}, function(response) { closeNotif(); return true; });				j++;			}		}	});*/	}document.addEventListener('load',function () {	if(document.getElementById('loaded').value == "false") {		document.getElementById('loaded').value = "true";		if(getArg('artist'))			document.getElementById('artist').innerHTML = decodeURIComponent(getArg('artist'));		if(getArg('song'))			document.getElementById('song').innerHTML = decodeURIComponent(getArg('song'));		if(getArg('coverurl'))			document.getElementById('albumCover').src = decodeURIComponent(getArg('coverurl'));		timeoutID = setTimeout(closeNotif, notifDuration * 1000);				document.getElementById('prev').addEventListener('click', function() {deezerControl('prev');}, true);		document.getElementById('pause').addEventListener('click', function() {deezerControl('pause');}, true);		document.getElementById('next').addEventListener('click', function() {deezerControl('next');}, true);				document.getElementById('body').addEventListener('mouseover', function() {handleMouseOver();}, true);		document.getElementById('body').addEventListener('mouseout', function() {handleMouseOut();}, true);	}}, true);