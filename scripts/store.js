var store = {	lastArtist: null,	lastArtistPopup: null,	lastSong: null,	lastSongPopup: null,	iTunesJSON: null,	contents: document.createElement("p").innerHTML = 'Lancez la lecture pour afficher les liens d\'achats.',	init: function(source) {		return true;	},	pushInformations: function() {		if(store.lastArtist != songData.songInf.currentArtist &&		store.lastSong != songData.songInf.currentSong) {			store.lastArtist = songData.songInf.currentArtist;			store.lastSong = songData.songInf.currentSong;			var xhr = new XMLHttpRequest(); 			xhr.onreadystatechange = function() {				if(xhr.readyState == 4) {					if(xhr.status == 200) {						store.iTunesJSON = JSON.parse(xhr.responseText);					}				}			};			xhr.open("GET", 'http://ax.phobos.apple.com.edgesuite.net/WebObjects/MZStoreServices.woa/wa/wsSearch?country=FR&media=music&entity=musicTrack&term=' + encodeURIComponent(songData.songInf.currentArtist) + '+' + encodeURIComponent(songData.songInf.currentSong) , true);			xhr.send(); 		}	},	refreshPopUp: function() {		chrome.extension.sendRequest({name: "getInfos", resource:"store"}, function(response) {			document.getElementById('store').innerHTML = response.content;		});	}}